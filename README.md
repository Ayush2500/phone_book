Install Node modules by running npm install on terminal.

To start the backend, use nodemon server.js.

To run the client, execute npm run dev.

You can also check the website, the code is already deployed
